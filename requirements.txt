# Monte Carlo Options Pricer — Python dependencies
# Install with:  pip install -r requirements.txt
#
# For CUDA acceleration also install:
#   conda install numba cudatoolkit   (recommended via conda)
# or:
#   pip install numba   (then ensure CUDA toolkit is on PATH)

numpy>=1.24
scipy>=1.11
yfinance>=0.2.40
numba>=0.59          # provides both CPU JIT and CUDA kernels
